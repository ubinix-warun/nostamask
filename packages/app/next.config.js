/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [ 'nostr.build',
        'robohash.org', 
        'upload.wikimedia.org', 
        'nostamask.infura-ipfs.io']
    }
}

module.exports = nextConfig
